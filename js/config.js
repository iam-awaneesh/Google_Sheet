// ── config.js ─────────────────────────────────────────────────────
// All app-wide constants live here.
// Change COLS / ROWS to resize the grid.
// ──────────────────────────────────────────────────────────────────

const COLS = 26;   // Number of columns (A–Z)
const ROWS = 50;   // Number of rows

// Default style applied to every new cell
const DEFAULT_STYLE = {
  fontFamily:    "'DM Mono', monospace",
  fontSize:      12,
  bold:          false,
  italic:        false,
  underline:     false,
  strikethrough: false,
  align:         'left',
  textColor:     '#e2e6f0',
  bgColor:       '#0f1117',
};
